# 🔧 Устранение проблем с деплоем

## Проблема: Сайт не обновляется после деплоя

### Шаг 1: Проверьте, запускается ли workflow

1. Откройте GitHub репозиторий
2. Перейдите в раздел **Actions**
3. Проверьте, есть ли запуски workflow "Deploy to VPS"
4. Если workflow не запускается:
   - Убедитесь, что вы пушите в ветку `main`
   - Проверьте, что файл `.github/workflows/deploy.yml` существует и закоммичен

### Шаг 2: Проверьте логи GitHub Actions

1. Откройте последний запуск workflow
2. Проверьте каждый шаг на наличие ошибок (красные крестики)
3. Частые проблемы:
   - **Ошибка SSH подключения**: Проверьте `VPS_SSH_KEY` в Secrets
   - **Ошибка сборки**: Проверьте, что `npm run build` проходит успешно
   - **Ошибка загрузки файлов**: Проверьте `VPS_HOST`, `VPS_USER`, `VPS_PATH`

### Шаг 3: Проверьте Secrets в GitHub

Убедитесь, что в **Settings → Secrets and variables → Actions** настроены:

- `VPS_HOST` - IP адрес сервера (например: `72.61.186.175`)
- `VPS_USER` - пользователь SSH (обычно `root`)
- `VPS_SSH_KEY` - приватный SSH ключ (полное содержимое ключа)
- `VPS_PORT` - SSH порт (обычно `22`)
- `VPS_PATH` - путь для деплоя (например: `/var/www/dosmundos/dist`)

### Шаг 4: Проверьте файлы на сервере

Подключитесь к серверу и проверьте:

```bash
# Проверьте наличие файлов
ls -lah /var/www/dosmundos/dist/

# Проверьте дату последнего изменения
stat /var/www/dosmundos/dist/index.html

# Проверьте информацию о деплое
cat /var/www/dosmundos/dist/.deployment-info
```

### Шаг 5: Проверьте конфигурацию Nginx

**ВАЖНО:** Nginx должен быть настроен так, чтобы отдавать статические файлы (включая `.deployment-info`) напрямую, минуя React Router.

```bash
# Проверьте конфигурацию
sudo nginx -t

# Проверьте, что Nginx указывает на правильную директорию
sudo cat /etc/nginx/sites-enabled/dosmundos.pe
# или
sudo cat /etc/nginx/conf.d/dosmundos.pe.conf

# Убедитесь, что в конфигурации есть правило для .deployment-info:
# location ~ ^/(\.deployment-info|sw\.js|vite\.svg|_redirects)$ {
#     try_files $uri =404;
#     add_header Cache-Control "no-cache, no-store, must-revalidate";
# }

# Если правила нет, добавьте его (см. nginx-config-example.conf)

# Перезагрузите Nginx вручную
sudo systemctl reload nginx
```

**Если файл `.deployment-info` не доступен:**
1. Убедитесь, что файл существует: `ls -la /var/www/dosmundos/dist/.deployment-info`
2. Проверьте права доступа: `chmod 644 /var/www/dosmundos/dist/.deployment-info`
3. Убедитесь, что в конфигурации Nginx есть правило для статических файлов (см. `nginx-config-example.conf`)

### Шаг 6: Очистите кеш

**На сервере:**
```bash
# Очистите кеш Nginx
sudo rm -rf /var/cache/nginx/*
sudo systemctl reload nginx
```

**В браузере:**
- Нажмите `Ctrl + Shift + Delete` (Windows/Linux) или `Cmd + Shift + Delete` (Mac)
- Очистите кеш и cookies
- Или откройте сайт в режиме инкогнито
- Или сделайте жесткую перезагрузку: `Ctrl + F5` (Windows/Linux) или `Cmd + Shift + R` (Mac)

### Шаг 7: Ручной деплой для тестирования

Если автоматический деплой не работает, можно задеплоить вручную:

```bash
# На вашем компьютере
npm run build

# Загрузите файлы на сервер
scp -r dist/* root@72.61.186.175:/var/www/dosmundos/dist/

# На сервере
ssh root@72.61.186.175
cd /var/www/dosmundos/dist
chmod -R 755 .
sudo systemctl reload nginx
```

### Шаг 8: Проверьте права доступа

```bash
# На сервере
sudo chown -R www-data:www-data /var/www/dosmundos/dist
sudo chmod -R 755 /var/www/dosmundos/dist
```

## Частые ошибки и решения

### Ошибка: "Permission denied"
**Решение:** Проверьте SSH ключ и права доступа на сервере

### Ошибка: "No such file or directory"
**Решение:** Убедитесь, что путь `VPS_PATH` существует и указан правильно

### Ошибка: "Nginx reload failed"
**Решение:** Проверьте конфигурацию Nginx: `sudo nginx -t`

### Сайт показывает старую версию
**Решение:** 
1. Очистите кеш браузера
2. Очистите кеш Nginx на сервере
3. Проверьте, что файлы действительно обновились на сервере

## Проверка статуса деплоя

Используйте скрипт `check-deployment.sh`:

```bash
chmod +x check-deployment.sh
./check-deployment.sh
```

Или проверьте вручную через браузер:
- Откройте `https://dosmundos.pe/.deployment-info` - там должна быть информация о последнем деплое

## Контакты для поддержки

Если проблема не решается:
1. Проверьте логи GitHub Actions
2. Проверьте логи на сервере: `sudo journalctl -u nginx -f`
3. Убедитесь, что все Secrets настроены правильно

