# Инструкция по добавлению новых эпизодов

Эта инструкция описывает процесс добавления новых эпизодов (транскрипций) в базу данных Supabase.

## 1. Подготовка файлов

Подготовьте JSON-файлы с транскрипциями. Файлы должны соответствовать следующему формату именования:

`YYYY-MM-DD_LANG_edit.json`

Где:
*   `YYYY-MM-DD` — дата эпизода (например, `2025-12-03`).
*   `LANG` — код языка (например, `ES`, `RU`, `EN`, `DE`, `FR`, `PL`).
*   `_edit.json` — обязательный суффикс.

**Пример:** `2025-12-03_ES_edit.json`

## 2. Размещение файлов

Создайте папку (например, `new-episodes`) в корне проекта или в любом удобном месте и поместите туда подготовленные JSON-файлы.

## 3. Запуск скрипта импорта

Откройте терминал в корневой папке проекта (`dosmundos-project`) и выполните следующую команду:

```bash
node scripts/import-episodes.cjs ./path/to/your/folder
```

**Пример:**
Если вы создали папку `new-episodes` в корне проекта:

```bash
node scripts/import-episodes.cjs ./new-episodes
```

## 4. Что делает скрипт

Скрипт автоматически выполняет следующие действия для каждого файла:
1.  **Читает файл:** Загружает содержимое JSON.
2.  **Определяет метаданные:**
    *   Извлекает дату и язык из имени файла.
    *   Формирует заголовок (например, "Медитация 03.12.2025").
    *   **Считывает длительность эпизода из аудиофайла** (если аудиофайл доступен по ссылке). Если аудио недоступно, берет длительность из транскрипции.
    *   Генерирует правильную ссылку на аудиофайл (`.../uploads/Audio/YYYY-MM-DD.mp3`).
3.  **Загружает в базу данных:** Создает новую запись или обновляет существующую (если эпизод с такой датой и языком уже есть).

## 5. Проверка

После завершения работы скрипта (вы увидите сообщение `🎉 Import finished`), новые эпизоды должны появиться на сайте.

## Примечания

*   Убедитесь, что в файле `.env` указаны правильные ключи `VITE_SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY`.
*   Аудиофайлы должны быть предварительно загружены на хостинг WordPress по адресу `.../wp-content/uploads/Audio/YYYY-MM-DD.mp3`. Скрипт только генерирует ссылки на них, но не загружает сами MP3 файлы.
