# 🔧 Быстрое исправление проблемы с деплоем

## Проблема: Сайт не обновляется после деплоя

### Решение 1: Настройте Nginx для отдачи статических файлов

**Проблема:** React Router перехватывает все запросы, включая запросы к статическим файлам.

**Решение:** Настройте Nginx так, чтобы он отдавал статические файлы напрямую.

1. **Подключитесь к серверу:**
   ```bash
   ssh root@72.61.186.175
   ```

2. **Проверьте текущую конфигурацию Nginx:**
   ```bash
   sudo cat /etc/nginx/sites-enabled/dosmundos.pe
   # или
   sudo cat /etc/nginx/conf.d/dosmundos.pe.conf
   ```

3. **Добавьте правило для статических файлов:**
   
   Найдите блок `server { ... }` и добавьте перед блоком `location / {`:
   
   ```nginx
   # Отдаем статические файлы напрямую (включая .deployment-info)
   location ~ ^/(\.deployment-info|sw\.js|vite\.svg|_redirects)$ {
       try_files $uri =404;
       add_header Cache-Control "no-cache, no-store, must-revalidate";
       access_log off;
   }
   ```

4. **Проверьте конфигурацию:**
   ```bash
   sudo nginx -t
   ```

5. **Перезагрузите Nginx:**
   ```bash
   sudo systemctl reload nginx
   ```

6. **Проверьте доступность файла:**
   ```bash
   curl https://dosmundos.pe/.deployment-info
   ```

### Решение 2: Проверьте, что файлы действительно обновляются

1. **Проверьте файлы на сервере:**
   ```bash
   ssh root@72.61.186.175
   ls -lah /var/www/dosmundos/dist/
   cat /var/www/dosmundos/dist/.deployment-info
   ```

2. **Проверьте дату последнего изменения:**
   ```bash
   stat /var/www/dosmundos/dist/index.html
   ```

3. **Если файлы старые, запустите деплой вручную:**
   - Откройте GitHub → Actions
   - Выберите "Deploy to VPS"
   - Нажмите "Run workflow"

### Решение 3: Очистите кеш

**На сервере:**
```bash
sudo rm -rf /var/cache/nginx/*
sudo systemctl reload nginx
```

**В браузере:**
- Нажмите `Ctrl + Shift + Delete` и очистите кеш
- Или откройте сайт в режиме инкогнито
- Или сделайте жесткую перезагрузку: `Ctrl + F5`

### Решение 4: Проверьте GitHub Secrets

Убедитесь, что в GitHub (Settings → Secrets and variables → Actions) настроены:
- `VPS_HOST` = `72.61.186.175`
- `VPS_USER` = `root`
- `VPS_SSH_KEY` = ваш приватный SSH ключ
- `VPS_PORT` = `22`
- `VPS_PATH` = `/var/www/dosmundos/dist`

### Быстрая проверка

После настройки Nginx, проверьте:
1. Откройте `https://dosmundos.pe/.deployment-info` - должен показать информацию о деплое
2. Если показывает 404, значит Nginx не настроен правильно
3. Если показывает информацию - значит деплой работает, но возможно проблема с кешем браузера

### Полный пример конфигурации Nginx

См. файл `nginx-config-example.conf` в репозитории.

