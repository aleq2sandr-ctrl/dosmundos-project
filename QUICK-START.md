# 🚀 Быстрый старт развертывания DosMundos

## Для быстрого запуска следуйте этому чек-листу:

### ✅ 1. Настройка VPS

```bash
# Подключитесь к вашему VPS
ssh root@72.61.186.175

# Загрузите и запустите скрипт установки
wget https://raw.githubusercontent.com/aleq2sandr-ctrl/dosmundos-project/main/install-vps.sh
chmod +x install-vps.sh
./install-vps.sh

# Настройте SSL сертификат
certbot --nginx -d dosmundos.pe -d www.dosmundos.pe
```

### ✅ 2. Настройка GitHub Secrets

В вашем GitHub репозитории добавьте эти секреты:

- **VPS_HOST**: `72.61.186.175`
- **VPS_USER**: `root`
- **VPS_SSH_KEY**: ваш приватный SSH ключ
- **VPS_PORT**: `22`
- **VPS_PATH**: `/var/www/dosmundos/dist`

### ✅ 3. Проверка домена

Убедитесь что DNS записи для `dosmundos.pe` указывают на `72.61.186.175`:

```bash
nslookup dosmundos.pe
```

### ✅ 4. Тестирование деплоя

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в ветку `main`
3. Проверьте Actions в GitHub - должен запуститься деплой
4. После успешного деплоя сайт будет доступен на https://dosmundos.pe

### ✅ 5. Мониторинг

```bash
# Проверьте статус приложения
pm2 status

# Посмотрите логи
pm2 logs dosmundos-web

# Проверьте что сайт отвечает
curl -I https://dosmundos.pe
```

## 📋 Контрольный список

- [ ] VPS настроен и работает
- [ ] SSL сертификат получен
- [ ] GitHub Secrets настроены  
- [ ] DNS записи настроены корректно
- [ ] Первый деплой выполнен успешно
- [ ] Сайт доступен по адресу https://dosmundos.pe
- [ ] Мониторинг настроен и работает

## 🔧 Полезные команды

```bash
# Перезапуск приложения
pm2 restart dosmundos-web

# Обновление SSL сертификата
certbot renew

# Просмотр логов Nginx
tail -f /var/log/nginx/access.log

# Проверка конфигурации Nginx
nginx -t

# Перезагрузка Nginx
systemctl reload nginx
```

**Готово!** Ваш сайт автоматически обновляется при каждом push в main ветку.
