# 🚀 Руководство по развертыванию DosMundos на VPS

Это полное руководство по настройке автоматического развертывания проекта DosMundos с GitHub на VPS сервер.

## 📋 Требования

- VPS сервер с Ubuntu 20.04+ 
- Настроенный домен `dosmundos.pe` с DNS A-записью, указывающей на IP сервера
- GitHub репозиторий с проектом
- SSH доступ к серверу

## 🔧 Настройка VPS

### 1. Подключение к серверу

```bash
ssh root@your-server-ip
```

### 2. Загрузка и запуск скрипта установки

```bash
# Загрузите файл install-vps.sh на сервер
scp install-vps.sh root@your-server-ip:/tmp/

# Подключитесь к серверу и запустите скрипт
ssh root@your-server-ip
chmod +x /tmp/install-vps.sh
/tmp/install-vps.sh
```

### 3. Настройка SSL сертификата

После установки настройте SSL сертификат:

```bash
certbot --nginx -d dosmundos.pe -d www.dosmundos.pe
```

Следуйте инструкциям для получения бесплатного SSL сертификата от Let's Encrypt.

## 🔑 Настройка GitHub Secrets

В вашем GitHub репозитории добавьте следующие секреты:

### Перейдите в Settings → Secrets and variables → Actions

Добавьте следующие секреты:

| Имя | Значение | Описание |
|-----|----------|----------|
| `VPS_HOST` | `72.61.186.175` | IP адрес вашего VPS |
| `VPS_USER` | `root` | Пользователь для подключения |
| `VPS_SSH_KEY` | Содержимое вашего приватного SSH ключа | SSH приватный ключ |
| `VPS_PORT` | `22` | SSH порт (по умолчанию 22) |
| `VPS_PATH` | `/var/www/dosmundos/dist` | Путь для развертывания |

### Генерация SSH ключа (если у вас его нет)

```bash
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"
# Сохраните ключ в ~/.ssh/dosmundos_key
```

Добавьте публичный ключ на сервер:

```bash
cat ~/.ssh/dosmundos_key.pub | ssh root@your-server-ip "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Скопируйте приватный ключ и добавьте его в GitHub Secrets как `VPS_SSH_KEY`.

## 🌐 Настройка домена

### DNS записи

Убедитесь, что у вашего домена `dosmundos.pe` настроены следующие DNS записи:

```
A    @    your-server-ip
A    www  your-server-ip
```

## 🚀 Автоматическое развертывание

### Принцип работы

1. При пуше в ветку `main` срабатывает GitHub Actions
2. Код клонируется на GitHub runners
3. Выполняется сборка проекта (`npm run build`)
4. Файлы из папки `dist/` загружаются на VPS
5. Статические файлы обслуживаются Nginx

### Ручной запуск

Для тестирования можно запустить workflow вручную:

1. Перейдите в Actions → Deploy to VPS
2. Нажмите "Run workflow"
3. Выберите ветку и запустите

## 🔍 Мониторинг и управление

### Проверка статуса приложения

```bash
# Статус PM2 процессов
pm2 status

# Логи приложения
pm2 logs dosmundos-web

# Перезапуск приложения
pm2 restart dosmundos-web
```

### Просмотр логов

```bash
# Логи приложения
tail -f /var/log/dosmundos/combined.log

# Логи Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Логи системы
journalctl -u nginx -f
```

### Обновление приложения

Обновления происходят автоматически при каждом push в ветку `main`. Для ручного обновления:

```bash
# Перейдите в директорию проекта
cd /var/www/dosmundos

# Остановите процесс
pm2 stop dosmundos-web

# Обновите файлы вручную (если нужно)
# Загрузите новые файлы dist/ сюда

# Перезапустите процесс
pm2 restart dosmundos-web
```

## 🔧 Безопасность

### Firewall

UFW настроен автоматически с правилами:
- SSH (22)
- HTTP (80) 
- HTTPS (443)

### Автоматические обновления

Включены автоматические обновления безопасности.

### Fail2ban

Установлен для защиты от брутфорса SSH.

## 🛠️ Устранение неполадок

### Проблемы с развертыванием

1. **GitHub Actions не запускается**
   - Проверьте настройки Secrets
   - Убедитесь что код пушится в ветку `main`

2. **Ошибки при загрузке файлов**
   - Проверьте SSH ключ и права доступа
   - Убедитесь что директория `/var/www/dosmundos` существует

3. **Nginx возвращает 404**
   - Проверьте что файлы загружены в `/var/www/dosmundos/dist`
   - Убедитесь что путь в конфигурации Nginx корректный

### Проблемы с SSL

1. **Let's Encrypt не может получить сертификат**
   - Проверьте что домен указывает на сервер
   - Убедитесь что порты 80 и 443 открыты

### Проблемы с производительностью

1. **Медленная загрузка**
   - Проверьте настройки кеширования в Nginx
   - Мониторьте использование ресурсов: `htop`, `df -h`

2. **Высокое потребление памяти**
   - Проверьте логи PM2: `pm2 logs dosmundos-web --lines 100`

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи на сервере
2. Убедитесь в корректности настроек домена и DNS
3. Проверьте статус сервисов: `systemctl status nginx`
4. Протестируйте подключение к серверу: `ssh root@your-server-ip`

---

**Важно**: Не забывайте регулярно обновлять сервер и создавать резервные копии данных.
